# Real 301s for the addresses that moved. Astro also emits an HTML redirect page for
# each of these; Cloudflare answers from this file first, so a crawler gets a status
# code instead of a meta refresh (owner asked for SEO, 2026-09-12).
# static rules first, then the placeholder ones, as Cloudflare documents.
/en                     /                         301
/en/structure           /structure/               301
/en/why                 /why/                     301
/en/adopt               /customize/               301
/en/failures            /failures/                301
/en/evidence            /failures/                301
/en/hooks               /hooks/                   301
/en/tech-stack          /tech-stack/              301
/en/skills              /skills/                  301
/en/architecture        /structure/               301
/evidence               /failures/                301
/kien-truc              /vi/cau-truc/             301
/cau-truc               /vi/cau-truc/             301
/vi-sao                 /vi/cach-tiep-can/        301
/vi/vi-sao              /vi/cach-tiep-can/        301
/cai                    /vi/tuy-bien/             301
/loi                    /vi/loi/                  301
/bang-chung             /vi/loi/                  301
/vi/bang-chung          /vi/loi/                  301

/en/skills/:slug        /skills/:slug/            301
/en/roles/:id           /roles/:id/               301
/vai/:id                /vi/role/:id/             301
/vi/vai/:id             /vi/role/:id/             301

# The standalone diagram pages moved from `explore/<slug>.html` to a directory, so the
# address is `/explore/<slug>/` like every other page. The old file address has been
# linked for months and is the one a bookmark holds, so it keeps a real 301 rather than
# a 404. The extensionless form needs no rule: Cloudflare resolves it to the directory.
/explore/:slug.html     /explore/:slug/           301
/explore/vi/:slug.html  /explore/vi/:slug/        301

# Install became Customize on 2026-09-14: the page always covered both, and the menu
# now says the half that matters. Both old addresses shipped and are linked from
# outside, so they keep a real 301.
/adopt                  /customize/               301
/vi/cai                 /vi/tuy-bien/             301
