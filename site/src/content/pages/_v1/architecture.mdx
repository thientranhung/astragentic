---
title: "Architecture — five roles, one ticket at a time"
description: "Astragentic runs five narrow roles, each with its own session boundary, passing a ticket through fixed phases from claim to merge."
---

import Eyebrow from '../../components/Eyebrow.astro';
import StepRail from '../../components/StepRail.astro';
import Diagram from '../../components/Diagram.astro';
import CompareGrid from '../../components/CompareGrid.astro';

<Eyebrow>Architecture</Eyebrow>

# Five roles. One ticket at a time.

Astragentic doesn't run one big agent. It runs five narrow roles, each with one job and its own session boundary, passing a ticket through fixed phases from claim to merge.

<Eyebrow>Roles</Eyebrow>

## Five sessions, five different lifespans.

Thomas stays resident and routes. Shaper holds one unbroken session from grill to tickets. Builder gets one session per ticket, in its own worktree. Rin reviews once per milestone, read-only. QA runs the product itself, once per walk.

<StepRail
  sequential={false}
  steps={[
    { name: "THOMAS", cadence: "resident", detail: "router — frontier, dispatch, arms, merge", badge: "shared" },
    { name: "SHAPER", cadence: "one per effort (unbroken)", detail: "grill → spec → tickets, no /compact", badge: "shared" },
    { name: "BUILDER", cadence: "one per ticket", detail: "implements + code-review + simplify, own worktree", badge: "worktree", emphasis: true },
    { name: "RIN", cadence: "one per milestone", detail: "verifies artifact AND process traces, read-only worktree", badge: "detached worktree" },
    { name: "QA", cadence: "one per walk", detail: "runs the real product — journeys + contracts", badge: "worktree" },
  ]}
/>

<Diagram slug="five-roles" />

<Eyebrow>Lifecycle</Eyebrow>

## One ticket, seven checkpoints.

Claim decides ownership before a worktree exists. Brief hands off the ticket. Five more steps follow in fixed order, each leaving an artifact — a commit, a marker, a line in the merge message.

<StepRail
  sequential={true}
  steps={[
    { number: 1, name: "Claim", artifact: "assignee written + read back", actor: "THOMAS" },
    { number: 2, name: "Brief", artifact: "dispatch into pane, at Base commit", actor: "THOMAS" },
    { number: 3, name: "Implement", artifact: "commits on ticket branch", actor: "BUILDER" },
    { number: 4, name: "Code-review", artifact: "both axes, one pass", actor: "BUILDER", group: "review-ladder" },
    { number: 5, name: "Simplify", artifact: "simplify(increment): commit, names the pass", actor: "BUILDER", group: "review-ladder" },
    { number: 6, name: "Cross-vendor arm", artifact: "arm(ticket): receipt", actor: "BUILDER", group: "review-ladder" },
    { number: 7, name: "Merge", artifact: "Ledger: line, assignee cleared", actor: "THOMAS" },
  ]}
/>

<Diagram slug="ticket-lifecycle" />

<Eyebrow>Review</Eyebrow>

## One round. Three layers. No loop.

Every ticket passes code-review, a simplify pass, and a cross-vendor arm — once, in order. At milestones, Rin adds one more gate. A blocking finding goes to the owner, never to another round.

*Prior system: 5–14 rounds. This ladder: one.*

<Eyebrow>Workspace</Eyebrow>

## Every agent gets its own pane.

One resident router. Several Builders, each in its own git worktree and its own terminal pane. Nothing shares a branch. Nothing shares a session.

<Diagram slug="hub-and-spokes" />

*Each Builder: its own pane, its own worktree, its own branch.*

<Eyebrow>Stack</Eyebrow>

## Four required. Two optional.

Claude Code is the root runtime — the milestone gate has no fallback. Everything else is an adapter: the tracker, the second vendor, the terminal workspace.

<CompareGrid
  columns={["Component", "Role"]}
  rows={[
    { label: "Claude Code CLI", values: ["Root runtime — every role can run here; the milestone gate has no fallback"], weight: "required" },
    { label: "Git (worktree support)", values: ["Isolation boundary — one worktree per Builder"], weight: "required" },
    { label: "herdr ≥ 0.8.0", values: ["Terminal workspace manager — agent panes"], weight: "required" },
    { label: "mattpocock-skills ≥ 1.2.3", values: ["The engineering method — wayfinder, grill, spec, tickets, implement, review"], weight: "required" },
    { label: "Codex CLI", values: ["Cross-vendor arm — a second AI reviews every ticket"], weight: "optional" },
    { label: "OpenCode CLI", values: ["Third runtime option for role dispatch"], weight: "optional" },
  ]}
/>

Exactly one issue tracker per project — GitHub Issues, Jira, or Linear. The harness does not run without one.

Installation is five steps and it's boring on purpose. See it on [`/install`](https://github.com/thientranhung/astragentic#quickstart), not here.
