---
title: "Compare — where Astragentic sits, not who wins"
description: "Astragentic is an orchestration layer, not a method. The engineering method is rented from mattpocock-skills, a declared dependency."
---

import Eyebrow from '../../components/Eyebrow.astro';
import Diagram from '../../components/Diagram.astro';
import Receipt from '../../components/Receipt.astro';
import CompareGrid from '../../components/CompareGrid.astro';
import StatTrio from '../../components/StatTrio.astro';

<Eyebrow>Compare</Eyebrow>

# Where Astragentic sits, not who wins

Astragentic is an orchestration layer, not a method. The engineering method — how a single session works — is rented from mattpocock-skills, a declared dependency. This page shows where that split puts every named tool.

<Eyebrow>The map</Eyebrow>

## Two layers, and only one is ours

Astragentic sits at the bottom, dispatching sessions. Runtimes — Claude Code, Codex, OpenCode — sit in the middle. Method sits on top, inside each session: mattpocock-skills is the one Astragentic rents.

<Diagram slug="layer-map" />

<Eyebrow>The difference</Eyebrow>

## A ticket, not a plan file

A single-session tool ends with a plan file in a branch. Astragentic ends with tickets on a tracker, wired by blocking edges — the structure that lets several agents work the frontier at once.

<Receipt kind="tracker-excerpt" source="issue tracker, frontier query" pending>
`TRA-142  blocked_by  TRA-139`
</Receipt>

<Receipt kind="plan-file" source="plan file, single session" pending>
`.claude/plans/2026-08-20-feature.md`
</Receipt>

<Eyebrow>Side by side</Eyebrow>

## Eight rows, same layer, no ranking

Columns, not cards — so nothing reads as a winner. Every row is something Astragentic's own docs or Superpowers' own release notes state about themselves, not a claim made about the other.

<CompareGrid
  columns={["Astragentic 2.7.13", "Superpowers 6.3.0"]}
  rows={[
    { label: "Layer", values: ["Orchestration; method rented (mattpocock-skills)", "Method + orchestration in one session"] },
    { label: "Work state", values: ["Issue tracker — frontier query, blocking edges, assignee-as-claim", "Plan file in the branch"] },
    { label: "Isolation", values: ["One worktree + one pane per ticket; several Builders run at once", ".worktrees/ inside the project; one controller calls subagents"] },
    { label: "Roles", values: ["5 roles with contracts — Thomas, Shaper, Builder, Rin, QA", "No roles; one controller"] },
    { label: "Review", values: ["4 layers: two-axis code-review → simplify → cross-vendor arm (Codex ↔ Claude) → Rin milestone gate", "1 reviewer per task (two verdicts) + 1 broad review at branch end"] },
    { label: "Product QA", values: ["dispatch-qa-walk — runs the built product, real journeys", "None"] },
    { label: "Runtimes", values: ["Claude Code, Codex, OpenCode", "12+ harnesses (Claude Code, Codex, Cursor, Copilot CLI, OpenCode, and others)"] },
    { label: "Release cadence", values: ["1–2 days; each patch fixes one measured defect", "4–6 weeks"] },
  ]}
/>

*Different layer, not a ranking — see the FAQ below for what that means for a toolkit you're already using.*

<Eyebrow>Convergence</Eyebrow>

## Two paths, the same shape

Both systems arrived at the same shape — worktree isolation, a fresh subagent per task, two-axis review — without copying each other. That is what the numbers below show.

<StatTrio
  items={[
    { value: "6", label: "MAJOR VERSIONS TO ARRIVE (SUPERPOWERS, 5.1.0 → 6.3.0)" },
    { value: "1", label: "ADR TO ARRIVE (ASTRAGENTIC, ADR 0001)" },
    { value: "3", label: "SHARED TRAITS, NO COPYING" },
  ]}
/>

<Eyebrow>FAQ</Eyebrow>

## Three questions people actually ask

**Do I have to stop using Superpowers or mattpocock-skills to use Astragentic?**
No stopping needed for mattpocock-skills — it's a required dependency, not an alternative. Superpowers works fine on its own, just not in the same repo as Astragentic: two orchestrators would compete for the same worktrees and tracker state.

**Does Astragentic replace Superpowers?**
No. They sit on different layers — Superpowers bundles method and orchestration into one session; Astragentic rents the method and coordinates many sessions across a tracker. Neither is a worse version of the other.

**Why not run both in the same repo?**
Both ship worktree tooling, subagent dispatch, and a SessionStart-style bootstrap. Running both means two schedulers writing to the same branches and two sets of instructions reaching every pane — not a feature conflict, a coordination collision.
