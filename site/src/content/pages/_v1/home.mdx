---
title: "Astragentic — several coding agents, one codebase, no collisions"
description: "Astragentic is the layer under your agents: isolation, dispatch, review, and a record of everything that ran."
---

import Eyebrow from '../../components/Eyebrow.astro';
import LaneDiagram from '../../components/LaneDiagram.astro';
import StatTrio from '../../components/StatTrio.astro';
import Receipt from '../../components/Receipt.astro';
import DefectCard from '../../components/DefectCard.astro';
import CompareGrid from '../../components/CompareGrid.astro';
import More from '../../components/More.astro';

<Eyebrow>Orchestration layer for coding agents</Eyebrow>

# Several coding agents. One codebase. <span class="accent">No collisions.</span>

Astragentic is the layer under your agents: isolation, dispatch, review, and a record of everything that ran.

<Eyebrow>The failure mode</Eyebrow>

## One checkout, two agents, one lost afternoon.

Two agents share a checkout. One moves `HEAD` while the other is mid-edit. A reviewer switches branch under the builder. Nothing errors — the work is just gone.

<DefectCard id="AST-016">
Agents sharing one checkout moved `HEAD` under each other. A read-only reviewer's branch switch landed mid-edit, and the change that was in progress was gone with no error to point at.
</DefectCard>

<More href="/why">See what else breaks →</More>
<Eyebrow>Isolation</Eyebrow>

## One ticket, one worktree, one pane.

Every Builder gets its own branch and its own checkout. Concurrency stops being a race — git decides the claim, and the tracker records who holds it.

<LaneDiagram
  lanes={[
    { ticket: "TRA-139", stages: ["claim", "build", "review", "merge"], activeStage: 1 },
    { ticket: "TRA-142", stages: ["claim", "build", "review", "merge"], activeStage: 2 },
    { ticket: "TRA-125", stages: ["claim", "build", "review", "merge"], activeStage: 0 },
    { ticket: "TRA-087", stages: ["claim", "build", "review", "merge"], activeStage: 3 },
  ]}
/>

<Eyebrow>What it does</Eyebrow>

## Four mechanisms, each leaving a trace.

<CompareGrid
  columns={["Isolation", "Dispatch", "Review", "Provenance"]}
  rows={[
    {
      label: "What it does",
      values: [
        "One worktree per ticket",
        "The tracker answers what is ready",
        "Two axes, one pass, then a second vendor",
        "Every commit says which pass ran",
      ],
    },
    {
      label: "Receipt",
      values: [
        { receipt: "workspace", pending: true },
        { receipt: "frontier-query", pending: true },
        { receipt: "arm-report", pending: true },
        { receipt: "git-log", pending: true },
      ],
    },
  ]}
/>

<More href="/architecture">How it fits together →</More>
<Eyebrow>Measured</Eyebrow>

<StatTrio
  items={[
    { value: "136", label: "FAILURE MODES, LOGGED" },
    { value: "5–14 → 1", label: "REVIEW ROUNDS" },
    { value: "3", label: "RUNTIMES" },
  ]}
/>

<More href="/evidence">Read the ledger →</More>
<Eyebrow>What this is not</Eyebrow>

## Four things Astragentic does not do.

- Not a model, not an agent CLI: it runs on Claude Code, Codex and OpenCode.
- Not the engineering method: the method is rented from `mattpocock-skills`. Astragentic orchestrates it.
- Not usable without a tracker: GitHub, Jira or Linear. The tracker is the coordination substrate, not a nice-to-have.
- Not proven end to end: its checks prove the tooling is correct. Nothing yet proves the whole loop.

<More href="/compare">Where it sits on the map →</More>
<Eyebrow>Getting in</Eyebrow>

Four commands and one agent-run installer. It needs Claude Code, git worktrees, herdr, and the `mattpocock-skills` plugin.

<Receipt kind="install-commands" pending />

<More href="https://github.com/thientranhung/astragentic#quickstart">Install guide →</More>
