---
import RoleBody from '../../../components/pages/RoleBody.astro';
import { ROLE_IDS, type RoleId } from '../../../lib/roles';

export function getStaticPaths() {
  return ROLE_IDS.map((id) => ({ params: { id } }));
}

const id = Astro.params.id as RoleId;
---

<RoleBody lang="vi" id={id} />
